{
  "name": "@locator/runtime",
  "version": "0.0.0",
  "main": "packages/runtime/dist/index.js",
  "files": ["packages/runtime/dist/**/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "publishPackages": "lerna publish"
  },
  "dependencies": {
    "@locator/shared": "0.1.5"
  },
  "devDependencies": {
    "lerna": "^5.2.0",
    "prettier": "^2.7.1",
    "turbo": "^1.3.4"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  }
}
